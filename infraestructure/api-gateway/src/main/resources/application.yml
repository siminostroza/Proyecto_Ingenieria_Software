spring:
  application:
    name: api-gateway
  config:
    import: "optional:configserver:http://config-server:8888/" # Esto también puede ir aquí si no se pasa por Docker
  profiles:
    active: docker # O el perfil que estés usando